<form method="post" action="{{ path('directeur_pedagogique_delete', {'id': directeur_pedagogique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ directeur_pedagogique.id) }}">
    <button class="btn">Delete</button>
</form>
